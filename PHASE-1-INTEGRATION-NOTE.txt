The calculation library is ready. The existing results page currently contains its own reference helper; the next code change should replace that duplicated calculation/reference logic with the centralized engine without altering report layout.